[phases.setup]
nixPkgs = ["python311", "zbar", "libzbar"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["echo 'Skipping build phase'"]

[start]
cmd = "gunicorn --worker-class eventlet --workers 1 --bind 0.0.0.0:$PORT wsgi:application"
