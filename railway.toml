[build]
builder = "railpack"

[deploy]
startCommand = "gunicorn --worker-class eventlet --workers 1 --bind 0.0.0.0:$PORT wsgi:application"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
